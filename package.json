{
  "name": "@lukekaalim/server",
  "version": "4.1.0",
  "description": "Simple utility library for defining a node server listener from a series of routes",
  "main": "src/main.js",
  "directories": {
    "example": "example"
  },
  "scripts": {
    "test": "node test",
    "preversion": "npm test",
    "postversion": "git push --follow-tags"
  },
  "author": "luke kaalim (luke@kaal.im)",
  "license": "MIT",
  "devDependencies": {
    "@lukekaalim/test": "^4.6.0",
    "flow-bin": "^0.133.0"
  }
}
